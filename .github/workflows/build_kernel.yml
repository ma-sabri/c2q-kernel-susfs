name: Build Note 20 Ultra (c2q) KernelSU-Next + SuSFS

on:
  workflow_dispatch:

jobs:
  build:
    name: Compile SM8250 Kernel
    runs-on: ubuntu-latest
    steps:
      - name: Maximize Build Environment & Install System Clang Toolchain
        run: |
          sudo rm -rf /usr/share/dotnet /etc/mysql /etc/php /usr/local/lib/android
          sudo apt update
          sudo apt install -y git cpio tar xz-utils bc bison flex libssl-dev make gcc g++ \
                              clang lld llvm gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu \
                              gcc-arm-linux-gnueabi binutils-arm-linux-gnueabi libelf-dev

      - name: Clone postkevone/c2q-kernelsu-next Source
        run: |
          git clone --depth=1 https://github.com/postkevone/c2q-kernelsu-next.git kernel
          cd kernel
          git submodule update --init --recursive || true


      
      - name: Apply SuSFS 4.19 Kernel Patches & Configure Defconfig
        working-directory: kernel
        run: |
          # 1. Fetch SuSFS 4.19 Patches
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b kernel-4.19 susfs

          # 2. Copy SuSFS core patches into kernel tree
          cp -r susfs/kernel_patches/include/linux/susfs* include/linux/ 2>/dev/null || true
          cp -r susfs/kernel_patches/fs/susfs* fs/ 2>/dev/null || true
          cp susfs/kernel_patches/fs/* fs/ 2>/dev/null || true
          cp susfs/kernel_patches/include/linux/* include/linux/ 2>/dev/null || true

          # 3. Patch KernelSU-Next with SuSFS support
          if [ -f "susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch" ]; then
            patch -p1 < susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch || true
          fi

          # 4. Locate defconfig from kernelsu-defconfig directory
          echo "[*] Searching for defconfig files..."
          find kernelsu-defconfig/ -type f | head -20
          
          # Find and copy the appropriate defconfig
          TARGET_DEFCONFIG=$(find kernelsu-defconfig/ -name "*kona*perf*" -o -name "*c2q*" | grep -E "(kona|c2q)" | head -n 1)
          
          if [ -z "$TARGET_DEFCONFIG" ]; then
            echo "[!] Warning: Could not find kona/c2q defconfig, searching for any perf_defconfig..."
            TARGET_DEFCONFIG=$(find kernelsu-defconfig/ -name "*perf*" | head -n 1)
          fi
          
          if [ -z "$TARGET_DEFCONFIG" ]; then
            echo "[!] Error: No suitable defconfig found!"
            exit 1
          fi
          
          echo "[✓] Found defconfig: $TARGET_DEFCONFIG"
          
          # Ensure arch/arm64/configs/vendor directory exists
          mkdir -p arch/arm64/configs/vendor
          
          # Copy defconfig to standard location
          cp "$TARGET_DEFCONFIG" arch/arm64/configs/vendor/kona-perf_defconfig
          echo "[✓] Copied to arch/arm64/configs/vendor/kona-perf_defconfig"

          # 5. Append SuSFS configuration
          cat << 'EOF' >> arch/arm64/configs/vendor/kona-perf_defconfig
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_SU=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          EOF
          
          echo "[✓] SuSFS configuration appended"

      - name: Compile Kernel Image with System Clang
        working-directory: kernel
        run: |
          mkdir -p out
          export ARCH=arm64
          export SUBARCH=arm64
          export CC=clang
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export HOSTCC=clang
          export HOSTCXX=clang++

          # Verify defconfig exists before build
          if [ ! -f "arch/arm64/configs/vendor/kona-perf_defconfig" ]; then
            echo "[!] Error: Defconfig not found at arch/arm64/configs/vendor/kona-perf_defconfig"
            exit 1
          fi
          
          echo "[✓] Defconfig verified"

          make ARCH=arm64 O=out CC=clang CLANG_TRIPLE=aarch64-linux-gnu- vendor/kona-perf_defconfig
          make ARCH=arm64 O=out CC=clang CLANG_TRIPLE=aarch64-linux-gnu- olddefconfig

          # Compile Image
          make -j$(nproc --all) ARCH=arm64 O=out \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            HOSTCC=clang \
            HOSTCXX=clang++ \
            Image      - name: Apply SuSFS 4.19 Kernel Patches & Configure Defconfig
        working-directory: kernel
        run: |
          # 1. Fetch SuSFS 4.19 Patches
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b kernel-4.19 susfs

          # 2. Copy SuSFS core patches into kernel tree
          cp -r susfs/kernel_patches/include/linux/susfs* include/linux/ 2>/dev/null || true
          cp -r susfs/kernel_patches/fs/susfs* fs/ 2>/dev/null || true
          cp susfs/kernel_patches/fs/* fs/ 2>/dev/null || true
          cp susfs/kernel_patches/include/linux/* include/linux/ 2>/dev/null || true

          # 3. Patch KernelSU-Next with SuSFS support
          if [ -f "susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch" ]; then
            patch -p1 < susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch || true
          fi

          # 4. Locate defconfig from kernelsu-defconfig directory
          echo "[*] Searching for defconfig files..."
          find kernelsu-defconfig/ -type f | head -20
          
          # Find and copy the appropriate defconfig
          TARGET_DEFCONFIG=$(find kernelsu-defconfig/ -name "*kona*perf*" -o -name "*c2q*" | grep -E "(kona|c2q)" | head -n 1)
          
          if [ -z "$TARGET_DEFCONFIG" ]; then
            echo "[!] Warning: Could not find kona/c2q defconfig, searching for any perf_defconfig..."
            TARGET_DEFCONFIG=$(find kernelsu-defconfig/ -name "*perf*" | head -n 1)
          fi
          
          if [ -z "$TARGET_DEFCONFIG" ]; then
            echo "[!] Error: No suitable defconfig found!"
            exit 1
          fi
          
          echo "[✓] Found defconfig: $TARGET_DEFCONFIG"
          
          # Ensure arch/arm64/configs/vendor directory exists
          mkdir -p arch/arm64/configs/vendor
          
          # Copy defconfig to standard location
          cp "$TARGET_DEFCONFIG" arch/arm64/configs/vendor/kona-perf_defconfig
          echo "[✓] Copied to arch/arm64/configs/vendor/kona-perf_defconfig"

          # 5. Append SuSFS configuration
          cat << 'EOF' >> arch/arm64/configs/vendor/kona-perf_defconfig
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_SU=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          EOF
          
          echo "[✓] SuSFS configuration appended"

      - name: Compile Kernel Image with System Clang
        working-directory: kernel
        run: |
          mkdir -p out
          export ARCH=arm64
          export SUBARCH=arm64
          export CC=clang
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export HOSTCC=clang
          export HOSTCXX=clang++

          # Verify defconfig exists before build
          if [ ! -f "arch/arm64/configs/vendor/kona-perf_defconfig" ]; then
            echo "[!] Error: Defconfig not found at arch/arm64/configs/vendor/kona-perf_defconfig"
            exit 1
          fi
          
          echo "[✓] Defconfig verified"

          make ARCH=arm64 O=out CC=clang CLANG_TRIPLE=aarch64-linux-gnu- vendor/kona-perf_defconfig
          make ARCH=arm64 O=out CC=clang CLANG_TRIPLE=aarch64-linux-gnu- olddefconfig

          # Compile Image
          make -j$(nproc --all) ARCH=arm64 O=out \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            HOSTCC=clang \
            HOSTCXX=clang++ \
            Image
      - name: Verify SuSFS Integration
        working-directory: kernel
        run: |
          echo "=== Auditing Compiled Kernel Binary for SuSFS ==="
          strings out/arch/arm64/boot/Image | grep -E 'susfs_add_sus_path|SUSFS_VERSION|SUS_PATH_HLIST' | head -n 8 || true

      - name: Upload Kernel Image Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Image-c2q-KernelSU-Next-SuSFS
          path: kernel/out/arch/arm64/boot/Image
